const state={category:"all",products:[],categories:[]};
const fa=n=>new Intl.NumberFormat("fa-IR").format(Number(n||0));
async function api(url,opts){const r=await fetch(url,opts);const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||"خطا در ارتباط با سرور");return d}
function renderCategories(){const el=document.querySelector("#categories");el.innerHTML=`<button class="chip ${state.category==="all"?"active":""}" data-cat="all">همه</button>`+state.categories.map(c=>`<button class="chip ${state.category===c.id?"active":""}" data-cat="${c.id}">${escapeHtml(c.name)}</button>`).join("");el.querySelectorAll(".chip").forEach(b=>b.onclick=()=>{state.category=b.dataset.cat;renderCategories();renderProducts()})}
function renderProducts(){const el=document.querySelector("#products-grid");let ps=state.products;if(state.category!=="all")ps=ps.filter(p=>String(p.category_id)===String(state.category));if(!ps.length){el.innerHTML='<div class="loading">فعلاً محصولی ثبت نشده است.</div>';return}el.innerHTML=ps.map(p=>`<article class="product"><div class="product-img">${p.image_url?`<img src="${escapeAttr(p.image_url)}" alt="${escapeAttr(p.name)}" loading="lazy">`:'<span class="no-img">بدون تصویر</span>'}</div><div class="product-body"><h3>${escapeHtml(p.name)}</h3><p>${escapeHtml(p.category_name||"")}</p><div class="price">${p.old_price?`<span class="old">${fa(p.old_price)} تومان</span>`:""}${fa(p.price)} تومان</div></div></article>`).join("")}
function escapeHtml(s){return String(s??"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[m]))}
function escapeAttr(s){return escapeHtml(s)}
(async()=>{try{const [c,p]=await Promise.all([api("/api/categories"),api("/api/products")]);state.categories=c.categories;state.products=p.products;renderCategories();renderProducts()}catch(e){document.querySelector("#products-grid").innerHTML=`<div class="loading">${escapeHtml(e.message)}</div>`}})();
